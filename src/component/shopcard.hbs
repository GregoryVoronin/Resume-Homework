<div class="col-4">
    <div class="card">
        <img src="{{image}}" alt="{{title}}" class="card-img-top">
        <div class="card-body">
            <div class="card-title"><span class="h4">{{title}}</span> {{>badge hot=isHot isRound=true}} {{>badge new=isNew isRound=true}}</div>
            <p>{{description}}</p>
        </div>
    </div>
</div>