<!DOCTYPE html>
<html>

  <head>
    <title>
      Resume | {{title}}
    </title>
    <meta charset='utf-8' />
    <link href='css/index.css' rel='stylesheet' />
  </head>

  <body>
    <header>
      {{#with header}}
        <div>
            <h1>
                {{#with name}}
                    {{firstname}} {{lastname}}
                {{/with}}
            </h1>
        </div>
        <div>
            <b>{{position}}</b>
            <div><em>{{salary}}</em></div>
            <br>
            <div><address>{{address}}</address></div>
        </div>
        <br>
        <hr>
      {{/with}}
    </header>

    {{>nav}}

    <main>
      {{{body}}}
    </main>

    {{>footer}}

  </body>
</html>